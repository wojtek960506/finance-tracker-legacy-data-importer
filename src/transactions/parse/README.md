CLI for preparing legacy transaction CSV files from exported spreadsheets.

Run the full pipeline from the repository root:

```bash
./src/transactions/parse/parse_legacy_data.sh
```

Parser inputs and outputs live under `data/transactions/parse/`.
